"use client";
import {useState} from "react";import {useRouter} from "next/navigation";
export default function PartnerForm({kind,existingIds}:{kind:"supplier"|"subcontractor";existingIds?:string[]}){const r=useRouter(),[saving,setSaving]=useState(false),[message,setMessage]=useState<string|null>(null);const supplier=kind==="supplier";async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);setMessage(null);const f=new FormData(e.currentTarget),g=(n:string)=>String(f.get(n)??"");const res=await fetch("/api/partners",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({kind,name:g("name"),specialty:g("specialty"),contact:g("contact"),phone:g("phone"),email:g("email"),address:g("address"),accountName:g("accountName"),accountNumber:g("accountNumber"),bank:g("bank"),status:g("status")})});const result=await res.json();setSaving(false);if(!res.ok)return setMessage(result.error);e.currentTarget.reset();setMessage(`${result.id} created.`);r.refresh()}const field=(label:string,name:string,required=true)=><div><label className="field-label">{label}</label><input name={name} required={required} maxLength={name==="phone"?15:undefined} className="field-input"/></div>;return <form onSubmit={submit}><p className="section-title">New {kind}</p><div className="mt-4 grid gap-4">{field(supplier?"Supplier name":"Subcontractor name","name")}{field(supplier?"Supply category":"Trade specialty","specialty")}<div className="grid grid-cols-2 gap-4">{field("Contact person","contact",false)}{field("Phone number","phone")}</div>{field("Email address","email",false)}{field("Address","address")}<p className="section-title mt-2">Bank details</p>{field("Account name","accountName")}<div className="grid grid-cols-2 gap-4">{field("Account number","accountNumber")}{field("Bank name","bank")}</div><div><label className="field-label">Status</label><select name="status" className="field-input"><option>Active</option><option>Inactive</option></select></div></div>{message&&<p className="mt-3 text-sm text-red-600">{message}</p>}<button className="btn-primary mt-6 w-full" disabled={saving}>{saving?"Saving…":`Create ${kind}`}</button></form>}