"use client";
import { useMemo, useState } from "react";
import EditPartnerRequest from "./edit-partner-request";
type Partner = any;
export default function PartnerDirectory({ rows }: { rows: Partner[] }) { const [query, setQuery] = useState(""); const [kind, setKind] = useState("All types"); const [status, setStatus] = useState("All statuses"); const filtered = useMemo(() => rows.filter(row => `${row.id} ${row.name} ${row.category} ${row.email ?? ""} ${row.phonenumber}`.toLowerCase().includes(query.toLowerCase()) && (kind === "All types" || row.kind === kind) && (status === "All statuses" || row.status === status)), [rows, query, kind, status]); return <div className="card overflow-hidden"><div className="flex flex-wrap gap-3 border-b border-slate-200 p-4"><input aria-label="Search suppliers and subcontractors" className="field-input min-w-[220px] flex-1" value={query} onChange={e => setQuery(e.target.value)} placeholder="Search name, code, category or contact…"/><select aria-label="Filter partner type" className="field-input max-w-xs" value={kind} onChange={e => setKind(e.target.value)}><option>All types</option><option>Supplier</option><option>Subcontractor</option></select><select aria-label="Filter partner status" className="field-input max-w-xs" value={status} onChange={e => setStatus(e.target.value)}><option>All statuses</option><option>Active</option><option>Inactive</option></select><button className="btn-secondary" onClick={() => { setQuery(""); setKind("All types"); setStatus("All statuses"); }}>Clear filters</button></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">Partner</th><th className="px-4 py-3">Type</th><th className="px-4 py-3">Category / specialty</th><th className="px-4 py-3">Contact</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{filtered.map(row => <tr key={row.id}><td className="px-4 py-3"><b>{row.name}</b><span className="block text-xs text-slate-400">{row.id}</span></td><td className="px-4 py-3"><span className="rounded-full bg-navy-50 px-2 py-1 text-xs text-navy">{row.kind}</span></td><td className="px-4 py-3">{row.category}</td><td className="px-4 py-3">{row.phonenumber}<span className="block text-xs">{row.email}</span></td><td className="px-4 py-3">{row.status}</td><td className="px-4 py-3"><EditPartnerRequest kind={row.kind === "Supplier" ? "supplier" : "subcontractor"} entityid={row.id} partner={{ phonenumber: row.phonenumber, email: row.email, address: row.address }}/></td></tr>)}{!filtered.length && <tr><td colSpan={6} className="px-4 py-10 text-center text-slate-400">No partners match the selected filters.</td></tr>}</tbody></table></div></div>; }
