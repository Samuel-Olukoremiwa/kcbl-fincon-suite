import { ContactRound } from "lucide-react";
import { createClient } from "@/lib/supabase/server";
import { requirePageAccess } from "@/lib/viewer";
import DirectoryWorkspace from "./directory-workspace";
export default async function DirectoryPage() { await requirePageAccess("projects"); const s = createClient(); const [{ data: clients }, { data: projects }, { data: managers }, { data: inflows }, { data: outflows }] = await Promise.all([s.from("clients").select("clientid,fullnameorcompanyname,clienttype,email,phonenumber,address,preferredpaymentmethod").order("clientid"), s.from("projects").select("projectid,clientid,projecttitle,projectlocation,projecttype,estimatedvalue,startdate,expectedenddate,status,projectmanageruserid").order("projectid"), s.from("users").select("userid,fullname").eq("usertype", "Staff"), s.from("cashinflowreceivables").select("projectid,amount,approvalstatus"), s.from("cashoutflowexpenditure").select("projectid,amount,approvalstatus")]); const sums = (rows: any[]) => rows.reduce<Record<string, number>>((all, row) => { if (row.projectid && row.approvalstatus === "Approved") all[row.projectid] = (all[row.projectid] ?? 0) + Number(row.amount); return all; }, {}); return <div><header className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-md bg-navy text-white"><ContactRound size={20}/></div><div><h1 className="text-xl font-semibold">Clients & project directory</h1><p className="text-sm text-slate-500">Client records with a focused financial snapshot for every project.</p></div></header><DirectoryWorkspace clients={clients ?? []} projects={projects ?? []} managerNames={Object.fromEntries((managers ?? []).map(x => [x.userid, x.fullname]))} inflows={sums(inflows ?? [])} outflows={sums(outflows ?? [])}/></div>; }
