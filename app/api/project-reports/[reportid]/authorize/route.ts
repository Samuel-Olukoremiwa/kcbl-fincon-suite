import { NextResponse } from "next/server";
import { getViewer } from "@/lib/viewer";
import { createAdminClient } from "@/lib/supabase/admin";
export async function PATCH(request:Request,{params}:{params:{reportid:string}}){const viewer=await getViewer();const{decision}=await request.json();if(viewer.department!=="MD Office"&&viewer.roleName!=="Super User")return NextResponse.json({error:"Only MD Office may authorize a reviewed progress report."},{status:403});if(!["Authorized","Rejected"].includes(decision))return NextResponse.json({error:"Invalid decision."},{status:400});const admin=createAdminClient();const{data:report}=await admin.from("projectreports").select("reviewstatus,progresspct").eq("reportid",params.reportid).maybeSingle();if(!report||report.reviewstatus!=="Reviewed"||report.progresspct===null)return NextResponse.json({error:"A supervisor review and progress percentage are required first."},{status:400});const{error}=await admin.from("projectreports").update({reviewstatus:decision,authorizedbyuserid:viewer.userId,authorizedat:new Date().toISOString()}).eq("reportid",params.reportid);return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true});}
