"use client";
import { useEffect, useState } from "react";
export default function CookieConsent() { const [open, setOpen] = useState(false); useEffect(() => setOpen(!localStorage.getItem("kcbl-cookie-consent")), []); if (!open) return null; const accept = () => { localStorage.setItem("kcbl-cookie-consent", "necessary"); setOpen(false); }; return <div className="fixed inset-x-4 bottom-4 z-50 mx-auto max-w-xl rounded-lg border border-slate-200 bg-white p-4 shadow-lg"><p className="text-sm font-semibold text-ink">Necessary cookies only</p><p className="mt-1 text-sm text-slate-600">This system uses essential session cookies to keep authenticated users signed in. It does not enable analytics or advertising cookies.</p><div className="mt-3 flex items-center gap-3"><a className="text-sm font-medium text-navy underline" href="/cookie-policy">Cookie policy</a><button className="btn-primary ml-auto" onClick={accept}>Accept necessary cookies</button></div></div>; }
